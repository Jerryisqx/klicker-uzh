query GetSingleGeneratedQuestion($id: Int!) {
  generatedQuestion(id: $id) {
    __typename

    ... on ChoicesElement {
      id
      name
      status
      type
      content
      explanation
      pointsMultiplier

      options {
        __typename
        hasSampleSolution
        hasAnswerFeedbacks
        displayMode
        choices {
          ix
          correct
          feedback
          value
        }
      }
    }

    ... on NumericalElement {
      id
      name
      status
      type
      content
      explanation
      pointsMultiplier

      options {
        hasSampleSolution
        accuracy
        placeholder
        unit
        restrictions {
          min
          max
        }
        solutionRanges {
          min
          max
        }
      }
    }

    ... on FreeTextElement {
      id
      name
      status
      type
      content
      explanation
      pointsMultiplier

      options {
        hasSampleSolution
        restrictions {
          maxLength
        }
        solutions
      }
    }

    ... on FlashcardElement {
      id
      name
      status
      type
      content
      explanation
      pointsMultiplier
    }

    ... on ContentElement {
      id
      name
      status
      type
      content
      explanation
      pointsMultiplier
    }
  }
}
